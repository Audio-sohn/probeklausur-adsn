

#include <vector>


/// @brief finds missing number in vector
/// @param vec 
/// @return 
int findMissingNo(std::vector<int> vec);

/// @brief creates a vector with numbers from 1 to n with a the number k missing
/// @return vector of int
std::vector<int> makeMissingNo(int n, int k);